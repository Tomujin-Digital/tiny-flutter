import 'package:flutter/material.dart';

class BattleBackgroundLayer extends StatelessWidget {
  const BattleBackgroundLayer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Color(0xffFFDEAD),
    );
  }
}
